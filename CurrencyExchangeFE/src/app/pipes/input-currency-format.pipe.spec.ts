import { InputCurrencyFormatPipe } from './input-currency-format.pipe';

describe('InputCurrencyFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new InputCurrencyFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
