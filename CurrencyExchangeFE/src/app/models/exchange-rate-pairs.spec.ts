import { ExchangeRatePairs } from './exchange-rate-pairs';

describe('ExchangeRatePairs', () => {
  it('should create an instance', () => {
    expect(new ExchangeRatePairs()).toBeTruthy();
  });
});
